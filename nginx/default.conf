server {
    listen 80;
    server_name _;

    # Serve ACME challenge files (keep this for future SSL attempts)
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Proxy to API (HTTP Mode)
    location / {
        proxy_pass http://vpn-api:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# SSL Configuration (Temporarily Disabled due to Rate Limiting)
# To enable HTTPS later:
# 1. Ensure you have valid certificates in /etc/letsencrypt/live/YOUR_DOMAIN/
# 2. Uncomment the block below
# 3. Replace REPLACE_WITH_DOMAIN with your actual domain

# server {
#     listen 443 ssl;
#     server_name REPLACE_WITH_DOMAIN;
#
#     ssl_certificate /etc/letsencrypt/live/REPLACE_WITH_DOMAIN/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/REPLACE_WITH_DOMAIN/privkey.pem;
#     
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     location / {
#         proxy_pass http://vpn-api:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
